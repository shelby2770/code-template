/**
 * Author: User
 * Date: 2024
 * License: CC0
 * Source: User-provided
 * Description: Common operations for arrays and multidimensional arrays.
 * Usage: See examples below
 * Time: O(n) where n is the number of elements
 */
#pragma once

// fill_n(&memo[0][0][0][0], sizeof memo / sizeof(int), INF);
// memset(memo, 0x3f, sizeof memo); //Set all ints to 0x3f3f3f3f (a large value ~1B)
